./ajtc.sh OneDrive_2026-05-28.3.zip
to run auuomated heck on given ZIP.

ZIP have to containe StudentWork from Sharepoint, not Wroking!

the plagirism-detector is used for showing plagiarism, it have seperate README.

configuration:
there MUST be a folder named "project" in which the JEST is configured, there must be a function.test.js for specific group.
ajtc.sh will automatically copy each student functions.js file into this project and run test.



There is also a PODMAN version for React JSX, but it have seperate configuration.